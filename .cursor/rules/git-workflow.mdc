---
description: bilibili-jump 直接推 master，修完默认 push + 重新打包
alwaysApply: true
---

# Git / 发布工作流

本仓库**不需要分支管理**：

- 改动在 `master` 上完成；**不要**默认开功能分支、不要默认建 PR
- 仅当用户明确要求开分支 / 开 PR 时才例外

## 默认收尾（不用每次再问）

检测修复、功能改动、review 通过或用户确认可用的改动完成后，默认依次：

1. 在 `master` 上 commit（必要时升 `package.json` version，须高于商店已发版本）
2. `git push origin master`
3. `npm run pack`，并给出发布包路径 `release/bilibili-jump-chrome.zip`

只有用户明确说「先别推 / 先别打包」时才跳过。
